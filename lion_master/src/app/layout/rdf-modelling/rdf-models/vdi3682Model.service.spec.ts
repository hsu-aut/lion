/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Vdi3682ModelService } from './vdi3682Model.service';

describe('Service: Vdi3682Model', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Vdi3682ModelService]
    });
  });

  it('should ...', inject([Vdi3682ModelService], (service: Vdi3682ModelService) => {
    expect(service).toBeTruthy();
  }));
});
