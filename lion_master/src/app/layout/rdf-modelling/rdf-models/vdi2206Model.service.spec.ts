/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Vdi2206ModelService } from './vdi2206Model.service';

describe('Service: Vdi2206Model', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Vdi2206ModelService]
    });
  });

  it('should ...', inject([Vdi2206ModelService], (service: Vdi2206ModelService) => {
    expect(service).toBeTruthy();
  }));
});
